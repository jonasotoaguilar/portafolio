import { describe, expect, it } from "vitest";

// Foundation smoke — verifies tooling, not portfolio content.
describe("foundation smoke", () => {
  it("builds the minimal route", () => {
    expect(true).toBe(true);
  });
});
