# Project-local pnpm secure config — no credentials.
# Verifies Node engine and pins exact versions per package.json.
engine-strict=true
save-exact=true
# Keep default registry over HTTPS; no auth tokens committed.
# strict-ssl remains true (default) — do not set to false.
