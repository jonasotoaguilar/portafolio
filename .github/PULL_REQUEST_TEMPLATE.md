<!-- Every PR should be linked to an issue with `status:approved`.
     PRs without a linked approved issue may be rejected during review.
-->

## Linked Issue

Closes #

## Summary

<!-- Provide a clear description of what this PR does and why. -->

## Changes

| File / Area    | What Changed      |
| -------------- | ----------------- |
| `path/to/file` | Brief description |

## Test Plan

<!-- List the actual commands you ran. Examples:
     pnpm run lint && pnpm run format:check
     pnpm run check
     pnpm run test:unit
     pnpm run build
     pnpm run test:e2e
-->

```bash
# commands you actually ran
```

- [ ] Format check passes (`pnpm run format:check`)
- [ ] Lint passes (`pnpm run lint`)
- [ ] Type check passes (`pnpm run check`)
- [ ] Unit tests pass (`pnpm run test:unit`)
- [ ] Static build succeeds (`pnpm run build`)
- [ ] E2E + axe passes when relevant (`pnpm run test:e2e`)

## Notes for Reviewers

<!-- Optional: anything reviewers should pay special attention to. -->
