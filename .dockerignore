# Keep the build context to source only: no build output, dependencies,
# VCS metadata, secrets, or local tool state. Nothing here is a secret —
# production credentials are never placed in the build context.

.git
.github
.agents
.sdd
.atl
.codegraph
.astro
dist
node_modules
.pnpm-store
coverage
playwright-report
test-results
.blob-report
.turbo
.vite
cache
openspec/changes/visible-motion-scroll-trace/traces
.env
.env.*
!.env.example
*.log
Dockerfile
.dockerignore
